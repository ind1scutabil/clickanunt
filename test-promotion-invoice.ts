/**
 * Legacy Test Script: Promotion & Invoice Generation
 *
 * This test depended on internal invoice modules that are not present in the repo.
 * It is intentionally a no-op to avoid build/lint failures.
 */

export async function testPromotionInvoice(): Promise<void> {
  console.warn('test-promotion-invoice: skipped (invoice modules not configured).');
}
